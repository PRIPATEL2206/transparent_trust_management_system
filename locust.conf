# Locust configuration
# Override via CLI flags or environment variables prefixed with LOCUST_

locustfile = loadtests/locustfile.py
host = http://localhost:8000
users = 50
spawn-rate = 5
run-time = 60s
headless = false
